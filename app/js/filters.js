'use strict';

/* Filters */
// Stub for now. 
// ENHANCEMENT / TODO - Perhaps add a filter to fuzzy match text against tweet content 
// or search any of multiple words.

// angular.module('tweetChallenge.filters', []).
// 	filter('interpolate', ['version', function(version) {
// 	return function(text) {
// 	  return String(text).replace(/\%VERSION\%/mg, version);
// 	};
// }]);
